#!/bin/bash

export TF_STATE=.
cd ${1}
echo "Run Benchmark ${1} ${2} ${3}"
export ssh_host=`terraform output client_ssh_host`
export ssh_host=`sed -e 's/^"//' -e 's/"$//' <<<"$ssh_host"`
scp -i ~/.ssh/pulsar_aws ec2-user@${ssh_host}:/opt/benchmark/\*.json ${2}

