apiVersion: 1

datasources:
  - name: "Prometheus"
    type: prometheus
    uid: prometheus
    access: proxy
    orgId: 1
    url: "http://{{ hostvars[groups['prometheus'][0]].private_ip }}:9090/"
    editable: true
    isDefault: true
    version: 1
    jsonData:
      timeInterval: 30s

